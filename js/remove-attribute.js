var firstItem = document.getElementById('one');     // Get the first item 
if (firstItem.hasAttribute('class')) {              // If it gas a class attribute 
  firstItem.removeAttribute('class');               // Remove its class attribute
}